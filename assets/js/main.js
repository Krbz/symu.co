var init = function(){
	var projectArea;
	var projectArea = document.getElementById('projectArea');

	// get pos of mouse to adding pins
	//
	// projectArea.addEventListener('mousemove', function(e){
	// 	console.log('X: '+(e.clientX-projectArea.offsetLeft) +' Y: '+ (e.clientY-projectArea.offsetTop) );
	// })

	// for (var i = 0; i < document.querySelectorAll('.pinContainer').length; i++) {
		// document.querySelectorAll('.pinContainer')[i].addEventListener('mousedown', function(e) {
			// e.which === 1 ? this.querySelector('.commentArea').classList.toggle('displayNone') : null;
		// })
	// };



}

window.onload = init;



